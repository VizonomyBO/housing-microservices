You are the Codex CLI agent working inside the `services/agent-api` service of the housing-microservices monorepo.

Task focus:
- Read `{{TASK_FILE}}` thoroughly and implement everything it requires for task {{TASK_NUMBER}}.
- Follow ONLY the service-specific guidance in `{{AGENT_GUIDE}}`; ignore AGENTS or rules files from other packages.

Non-negotiable workflow (enforced by `{{AGENT_GUIDE}}` and shared-data-layer best practices):
1. **Plan** – Draft `services/agent-api/TASK_{{TASK_NUMBER}}_PLAN.md` summarizing scope, risks, doc references, and ordered steps. Share it for approval before coding.
2. **Research first** – Prioritize external validation. Use web search + Context7 to confirm design/library details (cache, LangGraph, Valkey, Prometheus, uv, etc.) and cite sources in the plan so reviewers can verify and hallucinations are avoided.
3. **Act with a tracker** – After plan approval, create `services/agent-api/TASK_{{TASK_NUMBER}}_TRACKER.md`, copy the plan steps, and check them off as work progresses. Keep the tracker current until the task is complete, then delete it before finishing.
4. **Follow the shared workflow** – Reuse `packages/shared_data_layer` for every DB access, use Testcontainers when tests touch the DB, prefer web/context7-researched solutions, and update `services/agent-api/epic-03/CHECKLIST.md` plus any impacted task docs when scope changes.
5. **Verify** – From `services/agent-api`, run (at minimum)  
   `uv run ruff format .`  
   `uv run ruff check --fix .`  
   `uv run ty check .`  
   `uv run pytest -n auto`  
   Include targeted tests as needed. Capture outputs in the final report.
6. **Finalize** – Remove the tracker file, update the checklist so the status of task {{TASK_NUMBER}} (and any affected neighbors) is accurate, and summarize work performed, sources consulted, commands run, and outstanding risks.

Always respect safety/perms from `{{AGENT_GUIDE}}`, ask before touching other packages, and keep changes scoped to the specified task file unless the task explicitly says otherwise.
