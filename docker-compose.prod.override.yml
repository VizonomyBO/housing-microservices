services:
  auth-service:
    environment:
      AUTH_DATABASE_URL: ${AUTH_DATABASE_URL}

  user-service:
    environment:
      AUTH_DATABASE_URL: ${AUTH_DATABASE_URL}

  ingestion-service:
    environment:
      DATABASE_URL: ${HOUSING_DATABASE_URL:-${DATABASE_URL}}

  agent-api:
    environment:
      DATABASE_URL: ${DATABASE_URL}

  db-init:
    environment:
      DATABASE_URL: ${DATABASE_URL}
