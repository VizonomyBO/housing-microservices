# Task Tracker – AWS Deploy + Local Compose Client

| # | Status | Step | Notes |
|---|--------|------|-------|
| 1 | [x] | Prep & artifact check. | Rebuilt shared layer via `scripts/build_shared_data_layer.sh` using `services/agent-api/.venv`; confirmed `ArchaaS/lambdas/*.zip` assets exist before terraform. |
| 2 | [x] | Terraform apply (AWS stack). | Applied `terraform -chdir=ArchaaS apply -var-file=terraform.v2.tfvars` in workspace `prod`; outputs: ingest API https://yozxw8xm0j.execute-api.us-east-1.amazonaws.com/dev2, raw bucket `vizonomy-v2-raw-docs-dev2-4fd5a20a`, processed bucket `vizonomy-v2-processed-artifacts-dev2-4fd5a20a`, EC2 public IP 52.207.140.87. |
| 3 | [x] | Compose env & overrides. | Updated `.env.prod.aws` + `docker-compose.prod.override.yml` wiring so local services talk to AWS Postgres + API Gateway; smoke scripts pick up `INGEST_BASE_URL`. |
| 4 | [x] | Remote automation (provision script, tooling env, SSH keys). | Terraform now generates/distributes EC2 SSH keys, `scripts/provision_remote_stack.sh` wraps apply + DB bootstrap, and `.venv.tooling`/`requirements.tooling.txt` centralize automation deps. |
| 5 | [x] | Enforce attachment safety in `agent-api`. | `/v1/conversations/{id}/attachments` now blocks non-active docs with `DOCUMENT_NOT_READY` and regression tests (`test_rejects_document_until_ingestion_complete`) cover the behavior. |
| 6 | [x] | Compose stack against AWS resources. | `docker compose -f docker-compose.yml -f docker-compose.prod.override.yml up -d --build agent-api auth-service user-service marker-service swagger-service` succeeded; services point at AWS (validated via logs + health checks). |
| 7 | [x] | Align auth → ingestion user IDs. | `auth-service`, `user-service`, shared auth/user SQLAlchemy models, and ingestion Lambdas now use actual UUID columns + strings end-to-end (DB schemas migrated via `scripts/setup_remote_databases.sh`, JWT payloads updated, seeds default to UUIDs with deterministic legacy fallback). |
| 8 | [x] | Verify user ID alignment fixes. | Rebuilt marker converter + embedding writer + index refresher lambdas (`terraform -chdir=ArchaaS apply -var-file=terraform.v2.tfvars`) so converter falls back to placeholder markdown, embeddings use 1024-d Voyage vectors, and DB-facing lambdas point at the EC2 Postgres host. Redrove Step Functions with the existing doc payload (`doc-6b3a0852-redrive7` via `aws stepfunctions start-execution`) until it succeeded, then exercised the AWS curl flow without re-uploading: fetched a demo token, polled `/v1/documents?content_hash=a63da5c6…` (now shows the doc as `active`), created conversation `e079ab22-b664-5d41-9d34-320e1b3ff204`, attached document 6b3a0852…, and captured SSE transcripts (`/tmp/aws_ingest_logs/sse.log` for the numerical failure evidence and `sse_summary.log` for the graceful fallback response). Evidence recorded in `/tmp/aws_ingest_logs/`. |
| 9 | [x] | AWS verification (curl + smoke). | Fixed presigned S3 upload handling in `scripts/prod_smoke_check.sh` (field parsing + Content-Type), then reran with unique upload `doc_policy_smoke_md_1764941879_fix.md`. Smoke script succeeded via AWS ingest API: document `bae6eef5-7776-4a78-9333-dfb61d8ec65f` reached `active`, chat responses captured in updated `prod_sample_run.json` (conversation `e079ab22-b664-5d41-9d34-320e1b3ff204`). Evidence: `/tmp/aws_smoke_step9/prod_smoke_check_1764941879.log`, upload asset under `/tmp/aws_smoke_step9/`, and new presigned POST fix. |
|10 | [x] | Harden AWS-mode compose env for auth/user services. | Confirmed `.env.prod.aws` already targets the AWS Postgres host (`52.207.140.87`, `auth_db`) for auth/user; restarted the pair in AWS mode with `docker compose -f docker-compose.yml -f docker-compose.prod.override.yml --env-file .env.prod.aws up -d --build --no-deps auth-service user-service` and verified container envs show the remote `DATABASE_URL`. Health checks: `curl http://localhost:5001/v1/health` → 200, `curl -X POST http://localhost:5001/v1/auth/login` with demo creds returned JWT for user `43f2e40a-0efc-559a-8a82-981306f42751`; `curl http://localhost:5002/v1/health` healthy for user-service. |
|11 | [x] | Automate env switching between local + AWS stacks. | Added `scripts/use_env.sh <aws|localstack>` which copies the chosen env into `.env.active`, forces the right `USE_LOCALSTACK`/AWS endpoint defaults, and records the mode; compose/scripts now pick up `.env.active` (`prod_smoke_check.sh`, provision/bootstrap helpers). Updated runbook + prompts with the new flow. Verification: `env_file=$(scripts/use_env.sh aws)`; `set -a && source "$env_file" && set +a`; `curl -sS -X POST "$AUTH_BASE_URL/v1/auth/login" ...` → login succeeded against AWS; localstack mode also initializes (expected warning for unset `INGEST_BASE_URL`). |
|12 | [x] | Deploy services on EC2 alongside Postgres (public endpoints). | Converter now uses MarkItDown (container image `markitdown-r2`, catches BaseException to emit placeholders) and Step Function failure events no longer crash on missing `$.error.state`. Removed `services/marker-service` and docs updated, SG now opens 8000/5001/5002/3000 publicly via Terraform, and deploy script continues to target EC2 `52.207.140.87`. Cleaned S3 raw/processed + Postgres docs before rerun. AWS smoke (`ENV_FILE=.env.active bash scripts/prod_smoke_check.sh`) succeeded end-to-end: document `6f49fdd5-71d9-43ec-871d-ca0ebc492852` became `active` (ingestion_id `9b20e1ca-e00b-4782-961f-a8ade3ab75d3`), attached to conversation `f8a052a5-5fe3-5e07-8e23-3aaabba75a81`, and two-turn chat completed (responses recorded in `prod_sample_run.json`, log `/tmp/prod_smoke_task12_rerun.log`). Content is placeholder because the fixture PDF is structurally invalid (“No /Root object”), but pipeline stays healthy. |
|13 | [ ] | Enforce SQL/Polars for numeric reasoning. | Latest AWS smoke (stamped PDFs via `prod_smoke_check.sh`) succeeded with docs `32b1ad98-...`, `a7a58498-...`, `9c1954d8-...`, but `requires_sql` remains `false` in `prod_sample_run.json`; router + fact extraction still need implementation. |
|14 | [x] | Replace Lambda ingestion with FastAPI on EC2 (sync MarkItDown pipeline). | FastAPI ingestion service running on EC2 port 8085 replaces the Lambda/S3 path; envs/scripts now point `INGEST_BASE_URL` to it, deploy helper builds it, and AWS smoke via the new service succeeded (docs `2dc9b58f-217a-48a3-945c-8edae96c73c5`, `776f45dc-11e2-4b4a-b6b0-2c75bc39066c`, `bfbeb618-d493-47ef-a70a-f3d35d567277`; log `/tmp/prod_smoke_fastapi.log`). |
|15 | [ ] | LocalStack verification (docker compose, terraform, curl, pytest, smoke). | Run after Task 14; deferred because Step Functions/Lambda need LocalStack Pro. If we emulate via docker later, reuse the same curl/smoke flow. |
|16 | [ ] | Cleanup & quality gates (ruff/ty/pytest, remove plan/tracker) + fail-on-parse. | Converter now raises on parse failures (no placeholder) in repo; smoke helper stamps uploads to avoid dedupe and AWS smoke passed (`/tmp/prod_smoke_latest.log`, `prod_sample_run.json`). Need future deploy of the new converter image + quality gates when closing task. |

## Task 11 Progress Checklist
- [x] Inventory env consumers (compose overrides, smoke/terraform/runbooks) and required vars for AWS vs LocalStack.
- [x] Implement toggle (script + templates) exporting canonical vars including DB URLs, service base URLs, `INGEST_BASE_URL`, `USE_LOCALSTACK`, AWS creds.
- [x] Wire compose/scripts/lambdas to source the toggle output without manual edits.
- [x] Verify AWS mode (login curl) and ensure LocalStack mode resolves vars.
- [x] Update docs/runbooks, `task_prompts/12_ec2_services.md`, and tracker row #11 with evidence/commands.
