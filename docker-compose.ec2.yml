x-ec2-defaults: &ec2-defaults
  env_file:
    - ${ENV_FILE:-.env.prod}
  restart: unless-stopped
  networks:
    - ec2-net
  extra_hosts:
    - "host.docker.internal:host-gateway"
  logging:
    driver: "json-file"
    options:
      max-size: "100m"
      max-file: "5"

services:
  postgres:
    <<: *ec2-defaults
    image: pgvector/pgvector:pg16
    environment:
      PATH: /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/lib/postgresql/16/bin
      POSTGRES_USER: ${POSTGRES_USER:-vizonomy_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-housing}
      AUTH_DB: ${AUTH_DB:-auth_db}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - repo_postgres_data:/var/lib/postgresql/data
      - ./scripts/init-databases.sh:/docker-entrypoint-initdb.d/init-databases.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-vizonomy_user} -d ${POSTGRES_DB:-housing} -p 5432"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  init-migrations:
    <<: *ec2-defaults
    build:
      context: .
      dockerfile: docker/init-migrations/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-vizonomy_user}:${POSTGRES_PASSWORD:-postgres}@${DB_HOST_INTERNAL:-postgres}:5432/${POSTGRES_DB:-housing}
      DB_HOST: ${DB_HOST_INTERNAL:-postgres}
      DB_PORT: 5432
      DB_NAME: ${POSTGRES_DB:-housing}
      DB_USER: ${POSTGRES_USER:-vizonomy_user}
      REVISION: ${ALEMBIC_REVISION:-head}
    restart: "no"

  agent-api:
    <<: *ec2-defaults
    user: root
    build:
      context: .
      dockerfile: services/agent-api/Dockerfile.reduced
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-vizonomy_user}:${POSTGRES_PASSWORD:-postgres}@${DB_HOST_INTERNAL:-postgres}:5432/${POSTGRES_DB:-housing}
      INGEST_BASE_URL: ${AGENT_INGEST_BASE_URL:-http://ingestion-service:8085}
      INGEST_UPLOAD_API_KEY: ${INGEST_UPLOAD_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_CHAT_MODEL: ${OPENAI_CHAT_MODEL:-gpt-4o-mini}
      VOYAGE_API_KEY: ${VOYAGE_API_KEY:-}
      VOYAGE_EMBEDDING_MODEL: ${VOYAGE_EMBEDDING_MODEL:-voyage-context-3}
      VOYAGE_EMBEDDING_DIMENSIONS: ${VOYAGE_EMBEDDING_DIMENSIONS:-1024}
      VOYAGE_RERANK_MODEL: ${VOYAGE_RERANK_MODEL:-rerank-2.5}
      UVICORN_WORKERS: ${UVICORN_WORKERS:-1}
      AUTH_JWT_ISSUER: ${AUTH_JWT_ISSUER:-${AUTH_INTERNAL_BASE_URL:-${AUTH_BASE_URL:-http://auth-service:5000}}}
      AUTH_JWT_AUDIENCE: ${AUTH_JWT_AUDIENCE:-agent-api}
      AUTH_SHARED_SECRET: ${AUTH_SHARED_SECRET:-${JWT_SECRET_KEY:-}}
      AUTH_JWT_ALGORITHMS: ${AUTH_JWT_ALGORITHMS:-RS256,HS256}
      AUTH_JWKS_URL: ${AUTH_JWKS_URL:-}
      AUTH_JWKS_CACHE_SECONDS: ${AUTH_JWKS_CACHE_SECONDS:-3600}
      AUTH_JWT_LEEWAY_SECONDS: ${AUTH_JWT_LEEWAY_SECONDS:-60}
      AUTH_REQUIRED_SCOPES: ${AUTH_REQUIRED_SCOPES:-}
      AUTH_SCOPE_CLAIM: ${AUTH_SCOPE_CLAIM:-scope}
      AUTH_TENANT_CLAIM: ${AUTH_TENANT_CLAIM:-tenant_id}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000,http://localhost:5173}
      AGENT_API_CORS_ORIGINS: ${AGENT_API_CORS_ORIGINS:-http://localhost:3000,http://localhost:5173}
      AWS_REGION: ${AWS_REGION:-us-east-1}
      UV_CACHE_DIR: /tmp/uv-cache
    ports:
      - "${AGENT_API_PORT:-8000}:8000"
    command:
      - /bin/sh
      - -c
      - /app/services/agent-api/.venv/bin/uvicorn main:app --host 0.0.0.0 --port ${AGENT_API_PORT:-8000} --workers ${UVICORN_WORKERS:-1}
    depends_on:
      postgres:
        condition: service_healthy
      init-migrations:
        condition: service_completed_successfully
      ingestion-service:
        condition: service_started
      auth-service:
        condition: service_started
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s

  ingestion-service:
    <<: *ec2-defaults
    user: root
    build:
      context: .
      dockerfile: services/ingestion-service/Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-vizonomy_user}:${POSTGRES_PASSWORD:-postgres}@${DB_HOST_INTERNAL:-postgres}:5432/${POSTGRES_DB:-housing}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-}
      AUTH_BASE_URL: ${AUTH_INTERNAL_BASE_URL:-${AUTH_BASE_URL:-http://auth-service:5000}}
      VOYAGE_API_KEY: ${VOYAGE_API_KEY:-}
      VOYAGE_MODEL: ${VOYAGE_MODEL:-voyage-context-3}
      VOYAGE_OUTPUT_DIMENSION: ${VOYAGE_OUTPUT_DIMENSION:-1024}
      VECTOR_STORE_DIMENSION: ${EMBEDDING_DIMENSION:-1024}
      SIGNING_SECRET: ${SIGNING_SECRET:-${JWT_SECRET_KEY:-}}
      UV_CACHE_DIR: /tmp/uv-cache
      AWS_REGION: ${AWS_REGION:-us-east-1}
    ports:
      - "${INGESTION_SERVICE_PORT:-8085}:8085"
    depends_on:
      postgres:
        condition: service_healthy
      init-migrations:
        condition: service_completed_successfully
      auth-service:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8085/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s

  auth-service:
    <<: *ec2-defaults
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile
    ports:
      - "${AUTH_SERVICE_PORT:-5001}:5000"
    environment:
      AUTH_DATABASE_URL: postgresql://${POSTGRES_USER:-vizonomy_user}:${POSTGRES_PASSWORD:-postgres}@${DB_HOST_INTERNAL:-postgres}:5432/${AUTH_DB:-auth_db}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-}
      PORT: 5000
      AWS_REGION: ${AWS_REGION:-us-east-1}
      SES_REGION: ${SES_REGION:-us-east-1}
      SES_SOURCE_EMAIL: ${SES_SOURCE_EMAIL:-addis@vizonomy.com}
      SES_CONFIGURATION_SET: ${SES_CONFIGURATION_SET:-}
      PASSWORD_RESET_URL: ${PASSWORD_RESET_URL:-http://52.207.140.87/reset-password}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s

  user-service:
    <<: *ec2-defaults
    build:
      context: ./services/user-service
      dockerfile: Dockerfile
    ports:
      - "${USER_SERVICE_PORT:-5002}:5001"
    depends_on:
      postgres:
        condition: service_healthy
      auth-service:
        condition: service_started
    environment:
      AUTH_DATABASE_URL: postgresql://${POSTGRES_USER:-vizonomy_user}:${POSTGRES_PASSWORD:-postgres}@${DB_HOST_INTERNAL:-postgres}:5432/${AUTH_DB:-auth_db}
      AUTH_SERVICE_URL: ${AUTH_SERVICE_URL}
      PORT: 5001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  frontend-service:
    <<: *ec2-defaults
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile
      args:
        GITHUB_TOKEN: ${GITHUB_TOKEN:-}
        CACHEBUST: ${CACHEBUST:-93}
        VITE_PUBLIC_CONTENT_URL: ${VITE_PUBLIC_CONTENT_URL:-https://housing-public-content.s3.us-east-1.amazonaws.com}
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: 3000
      VITE_PUBLIC_CONTENT_URL: ${VITE_PUBLIC_CONTENT_URL:-https://housing-public-content.s3.us-east-1.amazonaws.com}
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  job-service:
    <<: *ec2-defaults
    build:
      context: ./services/job-service
      dockerfile: Dockerfile
    ports:
      - "${JOB_SERVICE_PORT:-8090}:8090"
    depends_on:
      agent-api:
        condition: service_healthy
      auth-service:
        condition: service_started
    environment:
      JOB_SERVICE_PORT: 8090
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      # Schedule disabled - reports are now cached indefinitely
      # Set JOB_SCHEDULE_ENABLED=true to re-enable monthly pre-generation
      JOB_SCHEDULE_ENABLED: ${JOB_SCHEDULE_ENABLED:-false}
      JOB_SCHEDULE_DAY: ${JOB_SCHEDULE_DAY:-25}
      JOB_SCHEDULE_HOUR: ${JOB_SCHEDULE_HOUR:-2}
      JOB_SCHEDULE_MINUTE: ${JOB_SCHEDULE_MINUTE:-0}
      JOB_RUN_ON_STARTUP: ${JOB_RUN_ON_STARTUP:-false}
      # API endpoints
      AGENT_BASE_URL: ${JOB_AGENT_BASE_URL:-http://agent-api:8000}
      AUTH_BASE_URL: ${JOB_AUTH_BASE_URL:-http://auth-service:5000}
      # Service account credentials (for job execution)
      JOB_SERVICE_EMAIL: ${JOB_SERVICE_EMAIL:-${PROD_DEMO_EMAIL:-}}
      JOB_SERVICE_PASSWORD: ${JOB_SERVICE_PASSWORD:-${PROD_DEMO_PASSWORD:-}}
      # Job config
      JOB_REPORT_ACCESS_SCOPE: ${JOB_REPORT_ACCESS_SCOPE:-base}
      JOB_REPORT_TIMEOUT_SECONDS: ${JOB_REPORT_TIMEOUT_SECONDS:-600}
      JOB_DELAY_BETWEEN_REPORTS: ${JOB_DELAY_BETWEEN_REPORTS:-5}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

  nginx:
    <<: *ec2-defaults
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./ArchaaS/configs/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - auth-service
      - user-service
      - agent-api
      - frontend-service
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

networks:
  ec2-net:
    driver: bridge
volumes:
  repo_postgres_data:
